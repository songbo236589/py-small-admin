# ===================================================================
# Docker 环境配置示例
# ===================================================================
# 说明:
# 1. 此文件仅包含 Docker 服务启动需要的配置
# 2. 复制此文件为 .env: cp .env.example .env
# 3. 应用配置请使用 server/.env.example
# 4. 修改密码时需同步更新 redis.conf 中的 requirepass
# ===================================================================

# ===================================================================
# 项目配置
# ===================================================================

# 项目名称（用于容器名前缀，可修改为其他名称）
PROJECT_NAME=py-small-admin

# ===================================================================
# MySQL 配置（Docker 服务）
# ===================================================================

MYSQL_ROOT_PASSWORD=root123456
MYSQL_ROOT_HOST=%
MYSQL_DATABASE=fastapi_db
MYSQL_USER=fastapi_user
MYSQL_PASSWORD=fastapi_password

# ===================================================================
# Redis 配置（Docker 服务）
# ===================================================================
# 注意：修改密码时需同步更新 redis/redis.conf 中的 requirepass

REDIS_PASSWORD=redis123456

# ===================================================================
# RabbitMQ 配置（Docker 服务）
# ===================================================================

RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=admin123
RABBITMQ_DEFAULT_VHOST=/

# ===================================================================
# 端口配置（Docker 服务宿主机暴露端口）
# ===================================================================
# 说明：格式为 "宿主机端口:容器端口"，容器端口固定不可修改
# 如需在同一台宿主机运行多个实例，可修改宿主机端口避免冲突

# MySQL 端口
HOST_PORT_MYSQL=3306

# Redis 端口
HOST_PORT_REDIS=6379

# RabbitMQ 端口
HOST_PORT_RABBITMQ_AMQP=5672
HOST_PORT_RABBITMQ_MGMT=15672

# FastAPI 端口
HOST_PORT_FASTAPI=8009

# Flower 端口
HOST_PORT_FLOWER=5555
