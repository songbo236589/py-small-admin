# ========================================
# 开发环境 Dockerfile
# ========================================
# 用途：本地开发，支持热更新
# 特点：包含Node.js运行时，挂载源代码
# ========================================

FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 安装系统依赖（如果需要）
RUN apk add --no-cache git

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm install

# 暴露开发服务器端口
EXPOSE 8000

# 设置环境变量
ENV NODE_ENV=development \
    UMI_ENV=dev \
    PORT=8000

# 启动开发服务器
CMD ["npm", "run", "start"]
